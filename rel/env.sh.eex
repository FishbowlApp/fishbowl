#!/bin/sh

ip=$(ip -j address show dev tailscale0 | jq -r '.[0].addr_info[] | select(.family=="inet") | .local')
export ERL_AFLAGS="-proto_dist inet_tcp -kernel prevent_overlapping_partitions false"
export ECTO_IPV6="true"
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="octo@${ip}"
